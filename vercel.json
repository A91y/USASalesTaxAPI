{
    "builds": [
      {
        "src": "USASalesTaxAPI/wsgi.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "USASalesTaxAPI/wsgi.py"
      }
    ],
    "buildCommand": "python manage.py migrate && python manage.py collectstatic --no-input && gunicorn locallibrary.wsgi:app --log-file -"
  }